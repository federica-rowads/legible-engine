# Copy to cloud-function/.env (gitignored) and fill in the three keys before deploying.
# deploy.sh reads this file, pushes each key into Secret Manager, and wires the function to it.
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=...
